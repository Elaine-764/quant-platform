# frontend temporarily keep track of selection: data, strategy, parameters
# feed into the engine.run() method via API
# engine does its logic
# api calls evaluation methods on result
# api calls visualization methods on result of evaluation and pnl
